<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
    <!--
      Local folder feed used to consume PowerCSharp Features packages during template development.
      Create it once:  mkdir -p ~/.powercsharp-local-nuget
      Then `dotnet pack` the PowerCSharp packages into it.
      TODO(Features): enable this source once the framework packages are published locally.
    -->
    <!-- <add key="powercsharp-local" value="~/.powercsharp-local-nuget" /> -->
  </packageSources>
</configuration>
